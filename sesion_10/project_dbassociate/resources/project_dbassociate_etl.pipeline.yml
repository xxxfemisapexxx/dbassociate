# The main pipeline for project_dbassociate

resources:
  pipelines:
    project_dbassociate_etl:
      name: project_dbassociate_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/project_dbassociate_etl"

      libraries:
        - glob:
            include: ../src/project_dbassociate_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
